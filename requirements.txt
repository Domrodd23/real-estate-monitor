# =============================================================================
# requirements.txt — dependency-light, grouped by build phase.
#
# Core (phases 1-8) is active now. Later groups are commented out so a fresh
# `pip install -r requirements.txt` stays small; uncomment a group when its
# phase lands.
# =============================================================================

# --- Core (phases 1-8): config, fetch, compute, dashboard --------------------
pandas>=2.0,<3
requests>=2.31
urllib3<2          # macOS system Python ships LibreSSL; urllib3 2.x warns/breaks on it
PyYAML>=6.0
python-dotenv>=1.0
jinja2>=3.1
plotly>=5.24        # need go.Choroplethmap (MapLibre tile basemap, no token)

# --- Phase 9: exports (PDF + Excel) ------------------------------------------
openpyxl>=3.1          # Excel (.xlsx) export
reportlab>=4.0         # pure-python PDF generation (no system libs required)
matplotlib>=3.7        # headless chart images for the PDF (no browser needed)

# --- Phase 10: forecast module -----------------------------------------------
statsmodels>=0.14      # ETS / Holt-Winters / SARIMAX time-series forecasts
scikit-learn>=1.3      # regularized / gradient-boosted driver model
